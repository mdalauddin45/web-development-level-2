{
  //
  const isAdmin = true;
  const message: string | number = isAdmin ? "Welcome admin" : "Welcome USER!";
  // console.log(message);

  const data = {
    name: "omuk",
    address: "dhaka",
  };

  // console.log(data?.name?.firstname); //optional chaining

  //
}
